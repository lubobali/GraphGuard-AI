# GraphGuard-AI

Real-time money laundering detection with a graph neural network.

> **Status: Phase 0 (ground work).** No model exists yet. This README grows into the
> full write-up in Phase 8. See `PLAN.md` for the build plan.

## The problem

Money laundering does not look suspicious one transaction at a time. It looks like a
**shape**: one account splits money across several, those funnel into one, sometimes
through a cycle, sometimes stacked in layers. Every individual hop is an ordinary
transfer for an ordinary amount.

That is why this is a graph problem. A row-level model structurally cannot see a shape
that spans six accounts.

**What the system does:** watches a stream of bank transfers and produces a ranked queue
of accounts worth investigating, fast enough to act on, with a reason attached to each.

**What it does not do:** decide anything by itself. A human investigator works the queue.
The model decides the *order*.

## The data is synthetic

This uses **IBM AMLworld**, generated by IBM's multi-agent simulator for a NeurIPS paper.
It is not real bank data, and that is stated up front rather than buried: no bank can
legally publish labeled laundering rings, which is why every paper in this area uses this
dataset. It ships labeled pattern files — the ground truth rings, by type — which is what
makes pattern-level recall measurable.

## The metric

**Primary: precision@k**, where k is how many alerts a team can actually work in a day.

Not AUC. At a 0.1% fraud rate you can score 0.99 AUC and still hand investigators a
worthless queue. Precision@k asks the only question that matters: of the k accounts we
told a human to look at, how many were really laundering?

Secondary: PR-AUC, and recall of *whole patterns* rather than individual edges — catching
1 transaction in a 12-hop ring is not catching the ring.

## Leakage contract

The full contract is in `CLAUDE.md` and is enforced by lint guards in CI, not by good
intentions:

1. Split by time, never randomly
2. The graph at time T contains only edges before T
3. No feature may use information that did not exist at decision time
4. The test window is opened once
5. A feature that looks too good gets investigated, not celebrated

## Development

```bash
./scripts/bootstrap.sh   # installs the project's own pinned uv, Python and just
./.tools/bin/just check  # lint + tests, exactly the way CI runs them
```

The toolchain lives inside the repo under `.tools/` and is pinned. Nothing is
shared with other projects on the host, so upgrading a tool elsewhere cannot
change a result here.
